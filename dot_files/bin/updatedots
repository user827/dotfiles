#!/bin/bash
set -eu

verify=--verify-signatures
if [ "$1" = --force ]; then
  verify=
fi

# --rebase ignores --verify signatures so turn back into merging
cd ~/dotfiles && git pull $verify && ./install.sh && ./init.sh "$@"
